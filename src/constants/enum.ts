export enum QUIZ_DIFFICULTY {
  EASY = "easy",
  MEDIUM = "medium",
  HARD = "hard",
}

export enum QUIZ_STATE {
  NOT_STARTED = "not-started",
  IN_PROGRESS = "in-progress",
  FINISHED = "finished",
}
